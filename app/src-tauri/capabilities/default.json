{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the app",
  "windows": ["main"],
  "permissions": [
    "dialog:default",
    "core:event:default",
    "core:window:default",
    "core:app:default"
  ]
}
